/**
 * Production environment — used by the Vercel build.
 * Update `apiUrl` to your deployed Render backend URL.
 *
 * Example after Render deploy:
 *   apiUrl: 'https://loadtrack-backend.onrender.com/api'
 */
export const environment = {
  production: true,
  apiUrl: 'https://loadtrack-backend.onrender.com/api'
};
